[pytest]
# Lets `pytest` work from the repo root as well as from backend/.
# Without pythonpath, `from app...` in the tests fails with ModuleNotFoundError
# unless you happen to be standing in backend/.
testpaths = backend/tests
pythonpath = backend
