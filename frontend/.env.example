# Usually leave this unset: the app calls /api/... on its own origin, which the
# vite dev proxy (dev) and nginx (Docker/EC2) forward to the backend.
# Set it only to point the app at a backend on a different host, e.g.
# VITE_API_BASE_URL=http://10.0.0.5:8000
VITE_API_BASE_URL=
